/** @type {import('next').NextConfig} */
const nextConfig = {
  // manifest.json, sw.js, icon-*.png, data.js, script.js, admin.html
  // otomatis ke-serve dari folder public/ tanpa perlu rewrite tambahan.
};

module.exports = nextConfig;
